# Orchestration Log: morpheus-3 (Architect / Technical Lead)

**Timestamp:** 2026-05-20T23:10:06.050-04:00  
**Agent:** morpheus-3 (claude-sonnet-4.6, background)  
**Task:** Resolve share-URL architecture + Azure storage setup; design landing pages  
**Status:** ✅ COMPLETED

## Work Summary

Morpheus delivered two major architecture decisions and one comprehensive technical setup guide:

### 1. Share-URL Architecture (Addressed Manish's Three Questions)

- **Scope:** Both source PDF AND translated PDF via private SAS URLs (30-day, read-only, per-file).
- **Storage:** Azure Blob `book-workspaces/` private container. Shape A friend-share via SAS tokens.
- **Metadata:** 14-field `metadata.json` schema including slug, landing_page_url, share block (SAS URLs, expiry, generation time).

### 2. WhatsApp Preview Decision (Option A Selected)

- **Decision:** Static HTML landing pages on Azure Static Website (`$web` container).
- **Why Option A:** Zero new infra (toggle on existing storage account). Shape A → Shape B continuity (same URL, SAS swaps to public). OG meta tags sync-rendered for scrapers.
- **Privacy posture:** Public-but-unindexed. `robots.txt` prevents Google indexing; WhatsApp/iMessage/Signal scrapers not affected (intended).
- **Landing page reveals:** Title, author, language pair, description, download links. NOT `license.status` (internal field).

### 3. Azure Storage Setup (Copy-Pasteable Commands)

- **Storage account:** `transposebooks` (globally unique name, Standard_LRS, TLS1_2).
- **Container:** `book-workspaces` (private, no public access).
- **Static Website:** Enabled on same account, base URL recorded, robots.txt uploaded.
- **Blob paths:** `book-workspaces/{slug}--{book_id}/input/source.pdf`, `output/translated.pdf`, `landing/index.html`, `metadata.json`, `glossary.json`.
- **SAS generation:** Per-file read-only, 30-day expiry.

### 4. Hard Constraint: `rights-unknown` at Creation (Four-Layer Enforcement)

1. **Database layer (Tank):** `license_status TEXT NOT NULL DEFAULT 'rights-unknown' CHECK (...)`.
2. **Application layer (Trinity):** Ingest function asserts post-insert `license_status == 'rights-unknown'`.
3. **Metadata.json layer (Trinity):** Workspace creation never accepts override.
4. **Test layer (Dozer):** Five binding tests covering each layer.

## Implementation Handoff

**Tank (T-1, T-2, T-3):**
- Run Azure setup command sequence (Section A)
- DB migration: add `license_status`, `metadata`, `provenance_source` columns
- Upload `robots.txt` to `$web/robots.txt`

**Trinity (TR-1, TR-2, TR-3, TR-4):**
- Enforce `rights-unknown` at ingest (remove override paths, add assertion)
- Update metadata.json writer (add slug, title, author, language fields)
- New pipeline stage: `generate-landing-page` (SAS generation, Jinja2 template rendering, $web upload)
- Optional translator_note prompt post-translation-complete

**Dozer (D-1, D-2, D-3):**
- Five license constraint tests (Section E)
- Four landing page tests (HTML meta tags, SAS URL readiness)
- Metadata.json schema validator

## Open Question (Minor)

**SAS expiry rotation:** (a) Manual CLI command vs (b) Scheduled nightly job. Recommendation: (a) now, (b) deferred. No blocker.

## Artifact

- `.squad/decisions/inbox/morpheus-share-url-and-preview-resolution.md` → merged into `decisions.md`

## Related Decisions

- 2026-05-20 Niobe: License status, private-until-verified
- 2026-05-20 Morpheus: Hybrid storage, blob-from-day-one
