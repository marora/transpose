# 2026-05-20T22:55:00-04:00 — Morpheus

**Agent:** Lead/Architect (Morpheus, Claude Sonnet 4.6)  
**Requested by:** Manish  
**Task:** Extend BookWorkspace schema with license + provenance fields  
**Output file:** `.squad/decisions/inbox/morpheus-workspace-license-provenance-fields.md`

## Completion Status

✅ **DECIDED** (4 open questions flagged, awaiting Niobe/Manish clarification)

## Summary

Morpheus extended the 2026-05-20 workspace architecture decision with two mandatory fields to support Niobe's product framing:

### 1. `license` Object

**Field:** `license.status` (enum)
- `rights-unknown` ← default at workspace creation (forces conscious upgrade)
- `claimed-public-domain` ← Manish's judgment, not legally verified
- `verified-public-domain` ← Manish researched source text + edition
- `rights-cleared` ← explicit permission from rights holder

**Mutability:** Mutable; all mutations appended to `license_history`.

### 2. `provenance.source` Object

```jsonc
{
  "url": "string | null",        // download URL of scanned PDF
  "edition": "string",           // publisher, edition, year
  "acquired_at": "ISO 8601",     // ingest timestamp (mandatory)
  "notes": "string | null"       // optional free text
}
```

**Mutability:** Mutable; corrections logged.

### Promotion-Eligibility Rule

```
license.status ∈ { "verified-public-domain", "rights-cleared" }
```

→ Only these two statuses qualify for Shape B public archive promotion. Enforced at storage layer (blob ACL), catalog layer (index filter), and pipeline (export/publish gate).

### Database Migration (Tank responsibility)

**Option B recommended:**
- Add `license_status TEXT NOT NULL DEFAULT 'rights-unknown'` with check constraint
- Add `provenance_source JSONB` for source object
- Add `metadata JSONB NOT NULL DEFAULT '{}'` for workspace contract

**Backfill:** Existing books → `license_status = 'rights-unknown'`, `provenance_source.url` from `source_blob_uri` (if URL-like), `acquired_at` from `books.created_at`.

### Implementation Handoff

- **Trinity (Pipeline):** Workspace creation sets `license.status = "rights-unknown"`, populates `provenance.source` from ingest params. Export/publish stage fails hard if license status ineligible.
- **Tank (Storage/Migration):** Write and run DB migration. Backfill existing rows. Enforce blob ACL policy per license status.
- **Dozer (Tests):** Unit tests for workspace creation defaults, promotion gate logic, license_history mutation logging, DB constraint enforcement.

### Open Questions (Flagging to Niobe/Manish)

1. **`claimed-public-domain` visibility:** Should Shape A "close friends" share include `claimed-public-domain` books, or only `verified-public-domain`? (Current rule keeps them fully private.)
2. **`verified-public-domain` criteria:** Add structured sub-field for evidence (publication year, jurisdiction, rationale) vs. relying on free-text `license.notes`?
3. **`rights-cleared` workflow:** Where to store signed permission artifact (e.g., `input/permission.pdf`)? Deferred but worth clarifying before Tank designs workspace layout.
4. **Multi-scan editions:** Can the same text exist in two workspaces (one `verified-public-domain`, one `claimed-public-domain`)? Current design assumes 1:1 workspace:source. Confirm assumption.

### Quality Signals

- Properly extends prior workspace decision (2026-05-20) without rewriting
- Aligns schema additions with Niobe's product framing (Shape A/B gate)
- Concrete migration path provided (Option B recommended)
- Flags architectural dependencies (Trinity mutation logging, Tank blob ACL, Dozer constraint tests)
- Open questions are legitimate architectural trade-offs, not oversights

### Architecture Decision Published

Entry merged to `.squad/decisions.md` (2026-05-20T22:52:32-04:00 timestamp preserved from decision log).

